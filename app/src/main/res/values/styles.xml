<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/dark_gray</item>
        <item name="colorAccent">@color/secondary</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Style for event name headline -->
    <style name="EventNameText" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style for general body text -->
    <style name="EventBodyText" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/secondary_text</item>
    </style>

    <!-- Style for price and date -->
    <style name="EventSubText" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/dark_gray</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Text color -->
        <item name="android:textColor">@android:color/white</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">@android:color/white</item>
        <!-- Underline color -->
        <item name="android:editTextColor">@android:color/white</item>
    </style>
</resources>